# favicon-wrangler
Retrieves and properly provides correct favicon for a given site that does not serve its favicon correctly for some password managers.

To run this application:

```
npm run dev
```
